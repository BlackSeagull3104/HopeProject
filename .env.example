# Optional DEVELOPMENT overrides. Built-in application constants already work.
# Desktop uses environment overrides or bundled constants; it does not read .env.
# Uncomment only the override you intend to supply; an empty override is an error.
# SEND_CODE_PROTOCOL_KEY=
# LOGIN_PROTOCOL_KEY=
# Never add passwords, phone numbers, SMS codes, cookies or tokens.
