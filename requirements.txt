# Pillow is used only to read local image dimensions for Markdown presentation.
Pillow>=10.0,<13

# Tkinter calendar cells, navigation and disabled-date styling.
tkcalendar==1.6.1

# Local document export (no external service).
python-docx==1.2.0
reportlab==4.4.10

# Bundled local CPU OCR, initialized only in a disposable worker.
rapidocr==3.9.2
onnxruntime==1.30.0
opencv-python==5.0.0.93
numpy==2.5.3

# Optional local semantic worker tokenizer; model is never bundled/downloaded at startup.
tokenizers==0.22.2
